- defaults:
    link:
        create: true
        relink: true

- clean: ['~']

- link:
    ~/.shell:
    ~/.tmux.conf:
    ~/.vim:
    ~/.vimrc:
    ~/.antigen:
    ~/.zshrc:
    ~/.config/nvim: nvim
- shell:
  - [git submodule update --init --recursive, Installing submodules]
