- defaults:
    link:
        create: true
        relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.shell:
    ~/.tmux.conf:
    ~/.vim:
    ~/.vimrc:
    ~/.zshrc:

- shell:
    - [git submodule update --init --recursive, Installing submodules]
    - 
        command: homebrew/install
        stdin: true
        stdout: true
