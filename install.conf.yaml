- defaults:
    link:
        create: true
        relink: true

- clean: ['~']

- link:
    ~/.shell:
    ~/.tmux.conf:
    ~/.vim:
    ~/.vimrc:
    ~/.antigen:
    ~/.zshrc:
    ~/.config/qutebrowser:
- shell:
  - [git submodule update --init --recursive, Installing submodules]
