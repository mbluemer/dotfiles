- defaults:
    link:
        create: true
        relink: true

- clean: ['~']

- link:
    ~/.shell:
    ~/.tmux.conf:
    ~/.vim:
    ~/.vimrc:
    ~/.antigen:
    ~/.zshrc:
    ~/.config/qutebrowser:
    ~/.i3:
- shell:
  - [git submodule update --init --recursive, Installing submodules]
